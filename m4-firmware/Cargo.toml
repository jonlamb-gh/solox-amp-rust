[package]
name = "m4-firmware"
version = "0.1.0"
publish = false

[dependencies]
cortex-m-rt = "0.6.3"
bare-metal = "0.2.3"

[dependencies.cortex-m]
version = "0.5.7"
features = ["const-fn"]

[profile.release]
codegen-units = 1 # better optimizations
debug = true
lto = true # better optimizations
